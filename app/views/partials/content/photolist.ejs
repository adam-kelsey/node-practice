<article class="display">
  <h1 class="display-title">Photography</h1>
  <p class="display-body">Here is all the wonderful photographs that has been submitted!</p>
  <div class="artdisplay">
    <% if (artwork.length > 0) { %>
    <% for(i=0; i < artwork.length; i++) { %>
      <img src="/images/artwork/<%= artwork[i]%>" alt="Artwork <%= i %>">
    <% } %>
    <% } %>
  </div>
</article> <!-- temporary photo filler until photos are separated from artwork -->